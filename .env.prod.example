# Production Environment Variables
# Copy this file to .env.prod and fill in the values

NODE_ENV=production
PORT=3000

# Database Configuration
# In docker-compose.prod.yml, the service name is 'postgres', so DB_HOST=postgres
DB_HOST=postgres
DB_PORT=5432
DB_USER=gym_admin
DB_PASSWORD=secure_password_change_me
DB_NAME=gym_db

# JWT Secret
JWT_SECRET=complex_secret_key_change_me_in_prod
